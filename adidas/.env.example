# Local XAMPP / MySQL configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=adidas_procurement

# Node/Express server
PORT=3000

# Optional: comma-separated frontend origin(s), or leave unset for local development
CORS_ORIGIN=http://localhost:3000

# Optional Gemini integration
GEMINI_API_KEY=
